# PolarSharp.EcommerceStorefronts

> Core storefront orchestration: IStorefrontClient + cart + checkout services. Lift-safe (zero PolarSharp.* deps outside BaseEntities + storefront feature family).

## Status

Scaffolded for v1.4.0; implementation in progress (Phase 24 of the v1.4.0 plan — see `docs/articles/...` once published).

## Install

```sh
dotnet add package PolarSharp.EcommerceStorefronts
```
