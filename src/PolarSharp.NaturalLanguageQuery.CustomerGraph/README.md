# PolarSharp.NaturalLanguageQuery.CustomerGraph

> CustomerGraphQuery target for natural-language → typed graph query generation.

## Status

Scaffolded for v1.3.0; implementation in progress (Phase 12 of the v1.3.0 plan — see `docs/articles/...` once published).

## Install

```sh
dotnet add package PolarSharp.NaturalLanguageQuery.CustomerGraph
```
