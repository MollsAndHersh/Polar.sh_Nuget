# PolarSharp.EcommerceStorefronts.SEO

> Server-side-rendered product pages + sitemap.xml + robots.txt + OpenGraph + JSON-LD + canonical URLs + hreflang.

## Status

Scaffolded for v1.4.0; implementation in progress (Phase 29 of the v1.4.0 plan — see `docs/articles/...` once published).

## Install

```sh
dotnet add package PolarSharp.EcommerceStorefronts.SEO
```
