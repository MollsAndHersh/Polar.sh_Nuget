# PolarSharp.NaturalLanguageQuery

> Abstractions for LLM-driven natural-language → query generation (audience-scoped schema slicing + authorization gate + cost guards).

## Status

Scaffolded for v1.3.0; implementation in progress (Phase 12 of the v1.3.0 plan — see `docs/articles/...` once published).

## Install

```sh
dotnet add package PolarSharp.NaturalLanguageQuery
```
