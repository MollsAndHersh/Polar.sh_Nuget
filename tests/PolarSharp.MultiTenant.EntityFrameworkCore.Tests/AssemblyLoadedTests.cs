namespace PolarSharp.MultiTenant.EntityFrameworkCore.Tests;

/// <summary>
/// Placeholder smoke test confirming the test project compiles and xunit picks it up.
/// Replaced with the real test suite in subsequent phases per the plan.
/// </summary>
public sealed class AssemblyLoadedTests
{
    [Fact]
    public void TestProject_Compiles()
    {
        Assert.True(true);
    }
}
